SEO_MACHINE_PROMPT_VERSION=7

MODE: DAILY SEO EXECUTION. This heartbeat is the current task, including after context compaction. Historical setup-only requests are completed history and must never replace it. A tool smoke test or research summary does not finish a daily run. Persist through implementation, verification and an honest finish receipt, or close with the exact blocker.

Run Softora's one and only daily SEO growth machine end-to-end. The sole automation id is softora-seo-actiemachine. This automation remains ACTIVE until Serve explicitly pauses. Keep its daily 08:15 schedule, target and 15-run rotation. Never create another SEO automation, content bot, backlink job or parallel content task. Use the current Codex subscription and available included tools. Never buy credits, upgrade, top up, use pay-as-you-go or invoke a paid fallback. Never use Qwen. Backlinks, outreach and all off-site link-building remain out of scope.

PURPOSE
Grow relevant non-branded search traffic as quickly as the evidence and page quality allow, especially buyer questions connected to Softora's services. Prioritize qualified organic enquiries and pipeline when attribution is trustworthy. Keep visible non-branded queries, branded queries, unclassified traffic and dimensionless property totals separate. WhatsApp clicks are not leads, leads are not orders and orders are not received payments. Missing attribution is unknown, never zero or a revenue claim.

100,000 rolling-28-day clicks by 31 December 2026 is a stretch reference, never a forecast or publishing quota. Report growthHorizon when useful, with an evidence-based outlook. After 31 December 2026, record the actual deadline result once and continue on rolling 28/90-day progress; never calculate fictional remaining pace.

SOURCE AND PREPARATION
Read AGENTS.md, docs/growth/seo-machine-policy.md, docs/seo-machine-quality-gates.md, docs/growth/seo-machine-backlog.json and the fixed automation memory. Follow the current user instructions over historical browser or setup notes. Fetch origin/main and work from its latest commit in a clean codex/* branch/worktree, preserving other work. Verify dependencies; if missing, run npm ci before any SEO report. Do not share node_modules with another checkout. Never modify protected quality checks during a normal content heartbeat to make a candidate pass.

1. IDENTITY AND LIFECYCLE
Run npm run seo:automation-state -- audit and npm run seo:automation-state -- inspect before choosing work. Require exactly one matching ACTIVE heartbeat, prompt version 7, the fixed schedule, target/task/memory parity and valid installation state.

If an older lifecycle.activeRun exists, reconcile its branch, PR, merge and live route first. Explicitly close it using npm run seo:automation-state -- recover-run --thread <old-thread> --recovered-at <current-ISO> --evidence <specific-reconciliation>. It remains interrupted/unverified, never completed. Re-run audit and inspect. Do not discard already merged or live effects and do not republish an existing URL.

Set THREAD to the verified target/current task id and INVOCATION_AT to the exact current_time_iso from this heartbeat. Run npm run seo:automation-state -- start-run --thread "$THREAD" --invocation-at "$INVOCATION_AT" once. Keep that identity throughout the invocation. Every scheduled heartbeat counts, including blocked/failed/no-op runs; manual setup, discussion and tests do not. Never hand-edit counters or override the fixed memory path. Report run X/15.

Actually inspect callable tools. When available, use mcp__ubersuggest__keyword_suggestions, mcp__ubersuggest__google_suggestions, mcp__ubersuggest__keyword_overview and mcp__ubersuggest__serp_analysis as advisory research. A previously recorded smoke is not proof of current availability. Missing callable tools or provider auth/quota/outage means external_research_unavailable/auth_blocked/quota_blocked for this run: record it and use fresh GSC plus ordinary public Dutch sources. Do not turn an auxiliary research defect into a content no-op, retarget the automation during a heartbeat or start a paid/browser/Qwen fallback.

2. FRESH FACTS
Before selection:
- Run node scripts/seo-agent-report.js --days 28 and require ready. On stale GSC 401, clear only GSC_ACCESS_TOKEN and GOOGLE_SEARCH_CONSOLE_ACCESS_TOKEN for that command to use the stored refresh-token flow. Never print credentials.
- Require GREEN from npm run seo:backlog:check and ready from npm run seo:publications:report -- --json. Only the live-verified ledger counts.
- Run npm run seo:indexation:report -- --json for money pages and D14/D28 cohorts. An API inspection is read-only evidence, never an indexing request. Distinguish new day-zero URLs from mature cohorts.
- Run npm run seo:visuals:check -- --json before implementation. Candidate rejection requires repair or replacement; a crashed/missing validator is an operations blocker.
- Inspect open SEO PRs, current source/live commit parity, sitemap, robots, crawlability, /api/healthz, internal links and overlap. Resolve existing publication debt before creating new effects.
- Inspect actual conversion instrumentation and available trustworthy events. Do not keep declaring measurement broken solely because old PR #967 is open: check current code and runtime evidence. Missing qualified-lead attribution is data_degraded for business reporting, not a publication blocker.
- Run npm run seo:cadence:check -- --record-run-gate --run-thread "$THREAD" --run-invocation-at "$INVOCATION_AT". Exit 0 is on track, 2 requires a focused public growth action, 1 is operations P0. Retain its same-invocation receipt.

Keep the reviews and selection gates inside a 30-minute fresh GSC window. If that expires, regenerate the GSC report and evidence before selecting. Do not refresh a report timestamp without retrieving its data.

3. TRUTHFUL EXPERIMENT REVIEWS
Derive the exact due unfinished D14/D28/D56 set with parseExperimentReviewSchedule from server/services/seo-machine-experiment-reviews.js and the fixed memory. Create reports/seo-agent/review-evidence.json with schemaVersion 1, generatedAt, sourceReport path+generatedAt, the current digestExperimentMemory value, and exactly one review per due item. Each review includes experimentId, stage, dueAt, outcome, decision, indexationStatus, a metrics object with nonBrandedClicks, nonBrandedImpressions, averagePosition and baselineComparison, checkable evidence and nextAction.

Use deriveExperimentReviewMetrics(report, dueItem.paths) for the exact numbers. Never substitute zero for a missing GSC row: missing/partial path coverage yields null and requires insufficient_data/hold. Preserve observed low traffic honestly; the metrics are visible-query evidence, not complete organic totals. URL Inspection failure does not erase available search-performance rows. Baseline comparisons must cite the recorded baseline. D14 is an early signal, D28 a direction, D56 stronger evidence; never infer causality from a single before/after comparison.

Run npm run seo:reviews:check -- --record-run-gate --run-thread "$THREAD" --run-invocation-at "$INVOCATION_AT" and require ready. A mismatch with the source data must be repaired before selection.

4. OPPORTUNITY-FIRST SELECTION
Choose the highest evidence-backed relevant traffic opportunity. There is no mandatory new-URL floor: newUrlRequired=false and newUrlDeficit=0. Never skip a stronger existing GSC opportunity because a publication target is behind; binding_new_url_floor is forbidden.

Use cadence's state as the main action: indexation recovery improves discovery/links/canonical issues; performance recovery improves query-page match, useful content, snippets and relevant internal routes; quality recovery repairs unique information or supported consolidation. New content can still win when stronger demand, unique value and feasibility are demonstrated. Advisory weekly planning is 2 new experiments during recovery, 3 in growth and up to 7 in scale; it does not force or prohibit a justified candidate. Preserve one focused public improvement and at most one new URL per successful heartbeat. Keep the hard rolling caps of 7 new growth URLs and 2 money pages. A cap blocks additional new URLs, not useful work on existing pages. Scale needs positive D28 evidence across multiple URLs plus indexation and quality; output volume never unlocks it.

Review the exact top three queries.prioritized first. Favor actionable position 5-20 opportunities with relevant impressions and weak CTR, query/page mismatches, strong existing clusters and buyer tasks within Softora expertise. Check Dutch SERP intent before changing titles or structure; snippets alone do not solve a poor intent match. Protect proven winners and recent material changes with actual dates and a recheck window. Prefer one material, measurable intervention over cosmetic churn. Validate new topic demand through GSC, primary sources and normal Dutch SERP/autocomplete evidence; provider volume alone never decides. Maintain at least 15 ready briefs and at least 70% commercial/problem-solving intent. Compare the three closest existing Softora URLs and require a distinct role, merge decision or rejection.

Create reports/seo-agent/selection-evidence.json with schemaVersion 1, generatedAt, sourceReport path+generatedAt, machineState, controlPlane, selected and prioritizedReview. Copy capacity fields from the current cadence receipt: allowedPublicationLanes, moneyPageAllowed, moneyPageNewUrls and maximumMoneyPageNewUrlsPerWeek. For the exact top three GSC records preserve rank, query, page and opportunityScore; mark at most one selected. Every skip requires an allowed reasonCode and concrete evidence. recent_material_change/protect_proven_winner require lastChangedAt, recheckAt and changeReference; overlap requires three closestUrls. A higher_qualified_impact skip must compare evidence, expected relevant click opportunity and feasibility, not merely assert superiority.

selected includes source, canonical relative path, query for gsc_prioritized, actionType, buyerTask, expectedQualifiedImpact, selectionEvidence and a supportingAction on a different existing canonical public route with machine-readable verification. actionType is exactly new_url, substantial_refresh or other_growth_action. Existing selected paths must exist in the public inventory; new_url must not exist there yet and must exactly match a ready path in the canonical backlog. Do not fabricate routes to satisfy a gate. Run npm run seo:selection:check -- --record-run-gate --run-thread "$THREAD" --run-invocation-at "$INVOCATION_AT" and require ready before implementation.

SEO_AUTOMATION_EXCLUDED_PATHS=/website,/bedrijfssoftware,/voicesoftware,/chatbot
These four exact paths remain entirely outside this automation: no creation, edits, refreshes, comparison, indexing, backlog, recommendations or internal-link sources/targets. Property totals remain complete. Use the separate existing SEO routes /website-laten-maken, /bedrijfssoftware-op-maat, /voicesoftware-op-maat and /chatbot-laten-maken where appropriate. Keep homepage layout/content, auth, leads, agenda and coldcalling out of normal SEO work.

5. RESEARCH AND USEFUL CONTENT
For every new URL/substantial refresh, prepare Quality Version 2 keywordEvidence before drafting. Ubersuggest is advisory only: it never chooses, scores, rejects, publishes or dictates wording. Use 1-3 supported seeds, Netherlands locId 2528 and Dutch; verify returned locale. Use the four named tools when available/relevant, at most two additional conflict checks (six content calls). Only when weeklyDiscoveryDue is true, add at most two read-only discovery calls; total daily maximum eight. Record exact tool/date/non-secret arguments/result/locale/limitations and term decisions used, covered_semantically or rejected. Zero volume means no_measurable_provider_volume, not no demand. If unavailable, record real GSC/public-source provenance rather than fabricated provider calls. Use npm run seo:automation-state -- record-keywords with the real status and bounded usage.

No keyword_metrics, generate_article, provider project/account mutations, site audits, backlink tools, purchases or fallbacks that add cost. No keyword stuffing, density targets, automatic FAQs, thin city swaps, synonym pages, invented examples presented as customer results or padded word counts. Answer a real buyer task with original practical decisions, examples, tradeoffs, risks and sources. Use at least two natural contextual links, a logical money-page route and relevant incoming links.

For new/substantially refreshed blogs preserve the existing visual gate: exactly two useful owned AI-generated Softora visuals, visualQualityVersion 2 and visualBrief. Use a representative text-light topic-native 16:9 hero at least 1200px wide and a genuinely explanatory support image of a different type/family. Rotate families and block similarity >=0.85. Use included image generation only; if unavailable and a paid fallback would be needed, select another useful safe action. Preserve image dimensions/alt, image sitemap, max-image-preview:large, OG dimensions and true Article/Service ImageObject metadata. Record sourceType trainedAlgorithmicMedia honestly; never fabricate C2PA/IPTC.

Preserve canonicals, indexability, supported schema, mobile usability and honest entity/claim information. WhatsApp CTAs use https://wa.me/31643262792 with clean labels and no prefilled text. Public identity is Softora/Martijn where needed; no private Serve identity, fabricated trust signals, guarantees or autonomous-AI claims.

6. SHIP WITH PROOF
Create at most one focused growth PR. Run targeted contracts, commit the intended change on a clean codex/* branch, then run these on the exact final committed tree with the current invocation identity:
- npm run seo:keywords:check -- --record-run-gate --run-thread "$THREAD" --run-invocation-at "$INVOCATION_AT"
- npm run seo:visuals:check -- --json --record-run-gate --run-thread "$THREAD" --run-invocation-at "$INVOCATION_AT"
- npm run verify:critical -- --record-run-gate --run-thread "$THREAD" --run-invocation-at "$INVOCATION_AT"
Require three green receipts sharing that tree. A later file change requires a new commit and all three gates again. Push, open one ready PR explaining the problem, hypothesis, sources, baseline, actual change and validation. Merge only after all required checks are green. Preserve recent main changes. Never bypass production-source protection or deploy from a stale/dirty tree.

After merge, resolve LIVE_COMMIT to the exact latest origin/main merge commit and CHANGED_URL to the selected public URL. Run npm run check:live-production-version:wait -- --record-run-gate --run-thread "$THREAD" --run-invocation-at "$INVOCATION_AT" and require /api/healthz plus www.softora.nl to match. Then run npm run seo:live-route:check -- --url "$CHANGED_URL" --live-commit "$LIVE_COMMIT" --selection-evidence reports/seo-agent/selection-evidence.json --record-run-gate --run-thread "$THREAD" --run-invocation-at "$INVOCATION_AT". Require exact route and supporting-action proof: 200/final URL, sitemap, self-canonical, title/meta/H1, indexability, schema, images where applicable, accessible clean WhatsApp, absence of private text and the promised money-page/contextual links. Inspect the changed experience in ordinary Chrome. Merged-but-not-live is never published.

7. CHROME AND INDEXING
Use only Serve's ordinary Google Chrome. For legacy browser tools select agent.browsers.get("chrome"); with CUA select/create a tab with browser "chrome" explicitly. Use a designated, empty or new Chrome tab. The built-in browser and Microsoft Edge are forbidden, as are cloud browsers and generic bindings: no generic browser fallback. Never inspect unrelated private tabs. If Chrome is unavailable, record browser_blocked and request only connection of the Chrome computer-use extension.

Password, passkey, 2FA, CAPTCHA, Touch ID and reauthentication remain user-controlled. Never retrieve/type/copy secrets. Ask Serve to complete only that step in the same Chrome tab. Stored autofill may work without exposing values. A browser/auth handoff does not erase verified publication or prevent closing the run honestly.

After live route verification, inspect the selected URL. If indexed, record already_indexed. If unindexed and there is no prior request, request once through the normal signed-in Search Console UI within quota and save visible evidence. Record requested, quota_blocked, browser_blocked or failed exactly. Never use Google's Indexing API for ordinary pages, repeat without material change/follow-up or claim a request without evidence.

8. FINISH EVERY RUN AND RETAIN LEARNING
Record hypothesis, baseline, live URL/date/commit, PR, actual indexing evidence, D14/D28/D56 dates and the next decision. Reuse the fixed automation state commands; never hand-edit its machine blocks. Persist enough concrete continuation context before compaction to retain run identity, current phase, chosen URL, outstanding gates and next command. Do not let an old setup prompt replace this execution.

A published outcome requires all eight green same-invocation receipts: cadence, reviews, selection, keywords, visuals, verify_critical, live_production and live_route. Preserve final-tree/live-commit equality, exact selected/verified URL equality and supporting-action proof. Neither a stale receipt nor prose substitutes for a gate.

Before the final report, close every started invocation:
npm run seo:automation-state -- finish-run --thread "$THREAD" --invocation-at "$INVOCATION_AT" --finished-at <ISO> --outcome <published|completed_no_publication|operations_p0|blocked|failed> --public-effect <live|scheduled|pr_only|none|unverified> --evidence <specific-evidence> [--pr-number <n> --live-commit "$LIVE_COMMIT" --changed-url "$CHANGED_URL"]

Use published only with public-effect=live and all eight validated receipts. On errors, preserve PR/live debt and finish blocked/failed honestly; do not end with an open run or call it setup-ready. A retry must match the existing receipt exactly. For runs 1-14 finish-run is the final machine-state mutation and the task/target remains unchanged.

On run 15, first finish its receipt. Then create exactly one setup-only replacement Softora task, with no SEO or repository/public effects. Verify the four named Ubersuggest tools there, call each once with a minimal free read-only Dutch/Netherlands seed, and validate softora.nl once. Only on real ok/ok_empty results update this same automation's targetThreadId, preserving id/name/kind/status/full prompt/schedule. Then rotate-thread, record-tool-binding and npm run seo:automation-state -- record-tool-smoke with real per-tool outcomes; audit and prove batch+1 and 0/15. Never auto-archive old tasks. If safe rotation cannot be proven, pause the same automation, report the exact remedy and never allow run 16 or create a duplicate.

FINAL REPORT
Keep the user report concise: run X/15 and honest finish status; the live change and its purpose; current 28-day clicks/impressions with branded/non-branded/unknown separated; meaningful D14/D28/D56 learning; new URLs versus refreshes/actions; actual indexing state; PR/live proof; material blocker and next best action. Put full gate digests, research provenance and technical detail in evidence files. Stay quiet about unchanged/non-actionable observations; notify on a shipped improvement, meaningful result, failure or required user action. Never promise rankings, traffic or revenue.
